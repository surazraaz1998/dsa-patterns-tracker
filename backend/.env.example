# Copy this file to .env for local (non-docker) runs.
# When using docker-compose, DATABASE_URL is already injected — you don't need this file.
DATABASE_URL=postgresql://dev:dev@localhost:5432/dsa_tracker
