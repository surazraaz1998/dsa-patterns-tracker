{
  "name": "dsa-patterns-tracker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm --prefix frontend ci && npm --prefix frontend run build"
  },
  "engines": {
    "node": "20.x"
  }
}
